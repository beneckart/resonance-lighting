# DOCUMENT 4 of 5 — ADDENDUM C: DJ/VJ + MIDI + Blender Workflow (CODE STARTS HERE)

**Part 1 — MIDI physical console.** Any class-compliant USB MIDI controller → cortex USB → ~50-line `mido` bridge → existing binary tweak path. Latency 1–3ms (faster than touch). Hardware: **Akai APC mini mk2 (~$120**, 9 faders + 64 RGB pads, dust-tolerant); **Midi Fighter Twister** (16 encoders with LED rings) for effect dials. Faders = brightness/speed/density/hue/sat/warmth; pads = cue bank with **bidirectional feedback** (pad blinks pending → solid when heartbeats confirm — the truth loop in your fingertips). Crossfader sweeps a light-trail A→B; jog wheel spins a ripple. Destructive ops require a two-button chord under the operator token. A = console on cortex / B = iPad-TouchOSC / C = lite page. P2: ESP32-S3 hosts USB natively → master could speak MIDI directly (survives cortex death).

**Part 2 — Guest DJ / AI VJ mode.** Hook-in: **~$15 USB line-in dongle** (DJ mixer booth-out → cortex, clean signal) + optional MIDI. iPad Guest DJ mode (toggle, level meter, guest scope — brightness caps, beacon/blackout locked out). **Two-layer brain:** Layer 1 reflexes (ms, pure DSP) — FFT bands → modulation buses (bass→brightness, highs→sparkle), beat onsets → schedule-ahead events; **drop detection** (buildup→collapse→burst) arms a "drop bomb" cue (all off → full-canopy explosion, beat-accurate). Layer 2 LLM VJ (seconds) — every 4–16 bars gets BPM/energy/section/crowd-occupancy digest, picks from the curated pattern vocabulary (circle-chase, sparkle, ripple, firefly, bloom, blackout-hold), patches the modulation matrix. Never done anywhere: an on-site LLM improvising over live audio + crowd state. Human override always wins; the LLM only *arranges* pre-designed patterns.

**Part 3 — Blender placement workflow.** Don't have Claude *use* Blender (clumsy) — have Claude *program* it (Python). Install **blender-mcp**, connect to **Claude Code**; Blender stays open, Claude executes live + screenshots its work; you art-direct, both watch, you validate. Three rules: scene-inventory dump every session; Mia's mesh in a locked collection scripts never touch; fixtures + skeleton in their own collections.

- **C3.2 Resonance Fixture Tools add-on** (Claude Code's first build): *Place* (add at cursor by role, snap-to-surface, distribute along ring/limb curve, mirror) · *Navigate* (Walk Mode for interior placement, saved viewpoints, frame selected) · *Light* (lumens field → Blender Watts, beam, CCT, Normalize All Lights, IES loader, **Playa Night** near-black world, global brightness fader) · *Data* (auto-ID naming, zone assign, count validator for 140-vs-150, export/import fixtures.json).
- **C3.3 Import audit (step zero):** dump collections/names/counts/bounding boxes → check scale (tree ≈ 7.5 units, not 7500 — the mm/m mismatch is why falloff looks insane), origin (base at world zero, Z-up), structure (did Rhino layers survive or merge to one blob?).
- **C3.4 Skeleton overlay (don't carve Mia's mesh):** trace named curves/empties (ring-1..N, limb-01..30, trunk-axis, root-door-N) → "add 4 lights at the middle ring" samples ring-2 at 4 points → place → snap → name → zone. Auto-name ~80% by spatial heuristics; you validate the rest. Disable selection on TreeMesh so clicks only hit fixtures/skeleton (the #1 frustration-killer).
- **C3.5 Lumens as a calibration field:** per-fixture lumens_max + beam_deg + role; Three.js renders physical units; an IES profile from Vishnu's lantern makes DIALux + twin shadows real; lumens_max maps brightness-% → PWM per fixture type; feeds the energy-truth math.
- **C3.6 Learning ladder:** discover in chat → write to CLAUDE.md (`#` shortcut) → promote to a Skill (SKILL.md auto-loads) → anything done >2× becomes an add-on button. End each session with "what did we learn?"
- **C3.8 Starter tasks:** (1) import-audit script + gap report; (2) Fixture Tools add-on v0; (3) skeleton overlay; (4) fixtures.json schema lock + round-trip; (5) R3F viewer scaffold loading glTF + fixtures.json. Open dependency: glTF export from Ed/Mia (or Blender-direct .3dm import of Mia's file). OQ-1 unchanged.
