# DOCUMENT 3 of 5 — ADDENDUM B: Controls, Doctrine, Failure Ladder + Continuation

**B1 — Existing-systems strategy ("author anywhere, compile to the tree").** One show-file format, many frontends. WLED = contingency fixture stack (steal effects, not platform). TouchOSC = day-one jam surface via OSC→param bridge. xLights converter + FPP scheduler model (sunset-relative). LedFx = audio-reactive reference. Home Assistant voice pipeline = pre-assembled Voice parts. Art-Net shim = any pro app as emergency console. DIALux = photometric validation via a shared IES profile. TouchDesigner = pattern prototyping → Show Compiler.

**B2 — Build + playa practice + testing.** Monorepo: `app/` (R3F+zustand+CBOR), `cortex/` (Python), `firmware/` (Ben's), `sim/` (WASM). Offline-first, red-shifted night UI, goggle/glove targets, ESD grounding, channel scan at placement, **config freeze after the Aug 1 soak**. Testing: codec round-trips; golden-frame parity (WASM sim vs real fixture render identical); finger-to-photon by 240fps camera (not vibes); PDR sweeps with DJI+LoRa active; chaos drills (kill cortex mid-jam, kill master mid-show, plug-pull ×20, forced beacon); RF-walk; door-counter ×100; 7-night soak.

**B3 — Flawless-without-wires doctrine.** teamLab: design so failure has nowhere to be seen — organic/CA patterns mask a dead fixture; diffuse causality; arbitrated interaction. Wristbands (PixMob/Xyloband) + GPS: stateless, repeated, open broadcast; dumb receivers; worst injection is cosmetic → **1Hz state beacon** (master radiates the full desired-state digest forever; any fixture converges within 1s; spoofing can't hold against a louder repeating truth). **Military PACE:** P ESP-NOW live · A compiled shows on synced clocks (zero-radio nights) · C LoRa low-rate · E physical (warden GPIO4). Optional 4th PHY: $0.30 IR receivers + crown emitters (RF-apocalypse-proof). Aviation lost-link rule: beacon silent → hold N min → ambient.

**B4 — Security, unencrypted by design.** Confidentiality is worthless ("turn green" isn't a secret); authority + self-healing are everything. Tiered by physics: Tier 0 open broadcast = playful scope (bounds-checked, self-heals ≤1s); Tier 1 operator = token on the private-AP WS; Tier 2 destructive (blackout/OTA) = hardware paths that never ride open radio. HMAC signing = optional nice-to-have. A hijacked light show is still a light show — optionally publish the playful frame format as a sanctioned instrument (radical inclusion as security model).

**B5 — Component A/B/C matrix.** pattern engine (firmware / WLED / compiled ambient) · jam (PWA / TouchOSC / lite page) · twin-monitor (PWA / lite table / LoRa digest) · schedule (cortex+compiler / master-flash / fixtures finish own clocks / ambient) · music (iPad / cortex mic / pre-timed cues) · Voice (Jetson / phone / off) · sensors (PIR→neighbors→sentinels+doors→schedule) · camp (Meshtastic / BM Internet / bike) · brain (Jetson / phone / none) · power (hat bank / shed ladder / USB power bank runs master for days) · commissioning (twin tap-bind / sticker map / RSSI clusters). Languages: fixtures C++, cortex Python, app TypeScript. UX: progressive disclosure (guest 3 controls → operator passcode unfolds the console); default = "tonight runs itself."

**B6 — Failure ladder + invariant.** The show lives in every fixture's flash, not on the radio. Dual-slot storage (A/B): a new show activates fleet-wide only after the hash gate. Plan A full → B master-only (show undiminished) → C total radio loss (fixtures finish on own clocks; promote spare master at dawn) → D floor (compiled ambient = 150 autonomous solar lanterns). Honest asterisk: a fixture that loses power mid-show with no master falls to ambient (fleet-unkillable, not per-fixture-unkillable).

**B7 — Industry pass.** Themed entertainment (Pharos/Alcorn McBride solid-state controllers, astronomical clock, autoplay-for-years; Disney "distribute timecode, not commands"; LumenRadio CRMX cognitive hopping validates channel agility; maintenance is the product). Subsea → **conformal-coat all 150 boards (~$1/board)** — best reliability-per-dollar in the corpus. Off-grid resorts → **Victron SmartSolar MPPT (~$100)** with serial telemetry to the warden. Solar streetlights → time-of-night **brightness envelope** as a schedule layer. Field of Light (Uluru) → design for the wipe-down.

**B8 — Visualizer platform thesis.** Tablet-native, hardware-true pre-viz + control in one tool doesn't exist in industry — that's our build. (1) **Field-based pattern authoring** — drag a ripple into the canopy, zero code, compiled to keyframes. (2) **Modulation matrix** — wind/temp/crowd/harvest as patchable buses on any parameter (env-intelligence you author, not hardcode). (3) **Voice as show designer** — "tree, feel like bioluminescent surf" → cortex composes a field cue → twin preview → lock. Future: versioned schemas, fixtures advertise engine version, venue addressing (2027 multi-installation), WebXR AR commissioning, open instrument + community show nights.

**B9 — Operator risk register.** (1) PIR window to Bali before fixtures ship (else PIR = 2027). (2) Mini-tree assembly before playa — first RF/commissioning rehearsal must not be build week. (3) Second trained operator + laminated runbook. (4) Voice 2am crisis handoff to Rangers/Zendo. (5) Beacon mode past BM-Org. Plus: maintenance-at-height, physical security (lantern mounts, conch theft, locked base box), 150-vs-140 count, spare iPad.

**CONTINUATION REPORT.** Re-boot: system_orientation → story_get(from_order 200, limit 200) → check pending handoffs → read this dossier. Pending: git-agent (branch elliots-controller, commit docs/research/, PR for Ben, don't merge — unconfirmed, may be blocked on byte access); portal-architect (signed share-URLs + mobile deeplink bug; share-links + git-info-section feature requests); task a4df10b2 (MIRA outreach to olena@olena.art / @ai_and_i.art — NOT olenaart.org). Working style: mobile, concise, act-now. Org = Resonance Studio. Web queue: PixMob IR repos, Pharos/Alcorn McBride sheets, LumenRadio CRMX, Victron VE.direct, AusChristmasLighting forums, WLED usermods, LedFx, esp-csi demo, Meshtastic playa reports, enclosure SKUs, Jetson DC spec + NX compat, three.js IES, WebXR commissioning, Sphere/WET Design talks.
