#pragma once

// Copy to wifi_secrets.h (gitignored), or pass this shape through
// build.sh --wifi-source <path>. Use the same shared maintenance WiFi as the
// fixtures; PUCA never starts its own access point.
#define RES_WIFI_SSID "your-network"
#define RES_WIFI_PASSWORD "your-password"
