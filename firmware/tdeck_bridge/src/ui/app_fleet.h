#pragma once
// Fleet operations app: stable roster/seen/live views, class and raw-VBAT
// filters, operator-selected sorting, node detail, exact-target identify, and
// a confirmed identify roll over the fresh rows in the current filtered view.
void appFleetOpen();
