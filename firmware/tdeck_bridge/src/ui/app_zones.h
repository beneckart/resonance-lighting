#pragma once
// Zones ("Hue-style"): class-targeted solid colors via the sustained stream
// service, fleet dark via lease, master dim scaled client-side (the wire's
// `bright` field is inert fixture-side — TODO.md → Firmware track).
void appZonesOpen();
