#pragma once

// LED Studio: class-targeted solid/blink colors via the sustained stream
// service. Master dim is scaled client-side (the wire's `bright` field is
// inert fixture-side -- TODO.md -> Firmware track).
void appZonesOpen();
